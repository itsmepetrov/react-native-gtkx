node_modules/
dist/
# examples/reanimated-dnd stages its two builds (the mirror and the real
# library) side by side, because `gtkx build` always writes to dist/.
dist-mirror/
dist-real/
out-tsc/
*.log
.DS_Store

# Local planning and machine-specific infrastructure — never committed.
# The exception: .claude/skills/ ships shared Claude Code skills for the repo.
.claude/*
!.claude/skills/
scripts/local/
