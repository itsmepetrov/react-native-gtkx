# The platform layer

This page moved. See [Architecture: Overview](architecture/overview.md) for
the `react-native-gtkx/gtk`, `/adw` and `/common` subpaths, and
[Architecture: Window, navigation, and settings](architecture/integration.md)
for navigation, actions, controllers and `GSettings`.
