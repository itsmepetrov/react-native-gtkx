import { AppRegistry } from "react-native"
import App from "./App"

AppRegistry.registerComponent("upstream-libraries", () => App)
AppRegistry.runApplication("upstream-libraries", {
  title: "Upstream libraries — react-native-gtkx",
  width: 900,
  height: 780,
})
