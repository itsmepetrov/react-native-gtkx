<?xml version="1.0" encoding="UTF-8"?>
<!--
  Preferences only. The tasks themselves are a DOCUMENT and live in a JSON
  file (src/storage.ts) — dconf is not a place to put user data. This is the
  app's SETTINGS store, which is what GSettings is for.
-->
<schemalist>
  <enum id="dev.rngtkx.tasksnav.SortOrder">
    <value nick="manual" value="0"/>
    <value nick="due-date" value="1"/>
    <value nick="title" value="2"/>
    <value nick="created" value="3"/>
  </enum>
  <schema id="dev.rngtkx.tasksnav" path="/dev/rngtkx/tasksnav/">
    <key name="sort-order" enum="dev.rngtkx.tasksnav.SortOrder">
      <default>'manual'</default>
      <summary>Sort order</summary>
      <description>How tasks are ordered in the list. Only "manual" allows drag-reorder.</description>
    </key>
    <key name="color-scheme" type="s">
      <choices>
        <choice value="default"/>
        <choice value="light"/>
        <choice value="dark"/>
      </choices>
      <default>'default'</default>
      <summary>Color scheme</summary>
      <description>Follow the system theme or force light or dark</description>
    </key>
    <key name="reminder-minutes" type="i">
      <range min="0" max="1440"/>
      <default>30</default>
      <summary>Reminder lead time</summary>
      <description>Minutes before a due time to show a reminder notification</description>
    </key>
  </schema>
</schemalist>
